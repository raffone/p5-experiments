// Generated by LiveScript 1.2.0
var colors, count, gradient, pos, px, dim, margin, zoom, colorRange, circleSize, setup, draw, mousePressed;
colors = [];
count = 0;
gradient = [];
pos = 1.7;
px = 8;
dim = 500;
margin = 80;
zoom = 0;
colorRange = 255;
circleSize = 128;
setup = function(){
  createCanvas(dim, dim);
  colorMode(HSB, 255);
  noFill();
};
draw = function(){
  var steps, slice, size, offset, i$, i, range, sin1, sin2, color;
  clear();
  background(0, 0, 245);
  steps = 32;
  slice = PI / steps;
  size = 420;
  offset = 40;
  for (i$ = 0; i$ <= steps; ++i$) {
    i = i$;
    range = (sin(i * slice - HALF_PI) + 1) * 0.5;
    sin1 = lerp((size / TWO_PI) * -1, size / TWO_PI, range);
    sin2 = lerp(0, size, range);
    color = (255 / steps) * i;
    bezier(size / 2 + offset, 0 + offset, sin1 + sin2 + offset, 0 + offset, sin1 + sin2 + offset, size + offset, size / 2 + offset, size + offset);
  }
  /*
  
  #console.log size / TWO_PI
  
  stroke 0
  ellipse (size / 2) + offset, (size / 2) + offset, size, size
  
  stroke 255, 0, 0
  
  # LEFT
  bezier (size / 2) + offset, 0 + offset,
         -28 + 0 + offset, 0 + offset,
         -28 + 0 + offset, size + offset,
         (size / 2) + offset, size + offset
  
  stroke 0, 255, 0
  
  # RIGHT
  bezier (size / 2) + offset, 0 + offset,
         28 + size + offset, 0 + offset,
         28 + size  + offset, size + offset,
         (size / 2) + offset, size + offset
  
  */
  /*
  # LEFT
  bezier (size / 2) + offset, 0 + offset,
         0 - (size / TWO_PI) + offset, 0 + offset,
         0 - (size / TWO_PI) + offset, size + offset,
         (size / 2) + offset, size + offset
  
  stroke 0, 255, 0
  
  # RIGHT
  bezier (size / 2) + offset, 0 + offset,
         size + (size / TWO_PI) + offset, 0 + offset,
         size + (size / TWO_PI) + offset, size + offset,
         (size / 2) + offset, size + offset
  */
};
mousePressed = function(){
  noLoop();
};