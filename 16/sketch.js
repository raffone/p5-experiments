// Generated by LiveScript 1.3.1
var tileSize, tileNumX, tileNumY, canvasWidth, canvasHeight, setup, draw, keyPressed, mousePressed;
tileSize = 32;
tileNumX = 19;
tileNumY = 12;
canvasWidth = tileSize * tileNumX;
canvasHeight = tileSize * tileNumY;
setup = function(){
  var i$, to$, x, j$, to1$, y, posX, posY;
  createCanvas(canvasWidth, canvasHeight);
  background(180);
  fill(255);
  for (i$ = 0, to$ = tileNumX; i$ <= to$; ++i$) {
    x = i$;
    for (j$ = 0, to1$ = tileNumY; j$ <= to1$; ++j$) {
      y = j$;
      posX = x * tileSize;
      posY = y * tileSize;
      console.log('x', x, 'y', y);
      fill(255);
      if (x === 9 && y === 6) {
        fill(200);
      }
      rect(posX, posY, tileSize, tileSize);
    }
  }
};
draw = function(){};
keyPressed = mousePressed = function(){
  return noLoop();
};