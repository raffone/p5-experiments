// Generated by LiveScript 1.2.0
var dim, baseX, baseY, baseZ, baseK, setup, draw, mousePressed;
dim = 400;
baseX = 0;
baseY = 0;
baseZ = 0;
baseK = 40;
setup = function(){
  createCanvas(dim, dim);
  background(0, 45, 90);
};
draw = function(){
  var x, y, z, i, F_x, F_y, xPI, yPI, _x, _y;
  clear();
  background(0, 45, 90);
  x = baseX;
  y = baseY;
  z = sin(baseZ++ * 0.01);
  i = 0;
  F_x = 0;
  F_y = 0;
  xPI = Math.PI / 90;
  yPI = Math.PI / 360;
  for (;;) {
    _x = round(sin(x++ * xPI + z) * 160 + 200);
    _y = round(cos(y++ * yPI - z) * 160 + 200);
    stroke(255, _y, _x);
    point(_x, _y);
    if (i === 0) {
      F_x = _x;
      F_y = _y;
    }
    if (i++ > 200 && F_x === _x && F_y === _y) {
      break;
    }
  }
  baseX += 0.4;
};
mousePressed = function(){
  noLoop();
};