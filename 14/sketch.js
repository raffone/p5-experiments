// Generated by LiveScript 1.2.0
var canvasWidth, canvasHeight, halfWidth, halfHeight, colorRange, setup, keyPressed, mousePressed;
canvasWidth = 400;
canvasHeight = 400;
halfWidth = canvasWidth * 0.5;
halfHeight = canvasHeight * 0.5;
colorRange = 255;
setup = function(){
  var steps, inc, points, i$, i, x, y, pairs, to$, len$, pair;
  createCanvas(canvasWidth, canvasHeight);
  background(0);
  fill(0);
  /*
  stroke 100
  ellipse halfWidth, halfHeight, height, height
  
  stroke 255
  ellipse halfWidth, halfHeight, 2, 2
  */
  steps = 44;
  inc = PI / (steps / 2);
  points = [];
  for (i$ = 0; i$ < steps; ++i$) {
    i = i$;
    stroke(255, 0, 0);
    fill(255, 0, 0);
    x = halfHeight + halfHeight * sin(inc * i);
    y = halfHeight + halfHeight * cos(inc * i);
    ellipse(x, y, 4, 4);
    points.push({
      x: x,
      y: y
    });
  }
  pairs = [];
  for (i$ = 1, to$ = points.length / 2; i$ < to$; ++i$) {
    i = i$;
    pairs.push({
      right: points[i],
      left: points[points.length - i]
    });
  }
  for (i$ = 0, len$ = pairs.length; i$ < len$; ++i$) {
    pair = pairs[i$];
    stroke(125);
    line(pair.left.x, pair.left.y, pair.right.x, pair.right.y);
    line(pair.left.y, pair.left.x, pair.right.y, pair.right.x);
    stroke(0, 255, 0);
    line(pair.left.x, pair.left.y, pair.right.y, pair.right.x);
    line(pair.left.y, pair.left.x, pair.right.x, pair.right.y);
  }
};
keyPressed = mousePressed = function(){
  return noLoop();
};