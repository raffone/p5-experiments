// Generated by LiveScript 1.2.0
var canvasWidth, canvasHeight, canvasMargin, halfWidth, halfHeight, circleR, colorRange, setup, draw, keyPressed, mousePressed;
canvasWidth = 300;
canvasHeight = 300;
canvasMargin = 50;
halfWidth = canvasWidth * 0.5;
halfHeight = canvasHeight * 0.5;
circleR = 100;
colorRange = 255;
setup = function(){
  createCanvas(canvasWidth, canvasHeight);
};
draw = function(){
  var steps, inc, points, i$, i, x, y, pairs, to$, ellipseHeightMolt, ellipseWidth, ellipseHeight, ellipseHeightOffset;
  clear();
  background(0);
  fill(0);
  stroke(255, 0, 0);
  ellipse(halfWidth, halfHeight, circleR * 2, circleR * 2);
  steps = 44;
  inc = PI / (steps / 2);
  points = [];
  for (i$ = 0; i$ < steps; ++i$) {
    i = i$;
    stroke(255, 0, 0);
    fill(255, 0, 0);
    x = circleR + circleR * sin(inc * i);
    y = circleR + circleR * cos(inc * i);
    points.push({
      x: x,
      y: y
    });
  }
  pairs = [];
  for (i$ = 1, to$ = points.length / 2; i$ < to$; ++i$) {
    i = i$;
    pairs.push({
      right: points[i],
      left: points[points.length - i]
    });
  }
  for (i$ = 0, to$ = pairs.length; i$ < to$; ++i$) {
    i = i$;
    /*
    stroke 125
    
    line pairs[i].left.x, pairs[i].left.y,
         pairs[i].right.x, pairs[i].right.y
    
    line pairs[i].left.y, pairs[i].left.x,
         pairs[i].right.y, pairs[i].right.x
    */
    stroke(0, 255, 0);
    fill(0, 0, 0, 0);
    ellipseHeightMolt = 0.2 + 0.4 * sin(frameCount * 0.01);
    ellipseWidth = circleR * 2 - pairs[i].left.x * 2;
    ellipseHeight = (circleR * 2 - pairs[i].left.x * 2) * ellipseHeightMolt;
    ellipseHeightOffset = ellipseHeight / 2;
    ellipse(circleR + canvasMargin, pairs[i].left.y + canvasMargin, ellipseWidth, ellipseHeight);
    /*
    stroke 0 255 0
    
    line pairs[i].left.x, pairs[i].left.y,
         pairs[i].right.y, pairs[i].right.x
    
    line pairs[i].left.y, pairs[i].left.x,
         pairs[i].right.x, pairs[i].right.y
    */
  }
};
keyPressed = mousePressed = function(){
  return noLoop();
};