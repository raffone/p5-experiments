// Generated by LiveScript 1.2.0
var basePos, baseDim, setup, draw, mousePressed;
basePos = 0;
baseDim = 0;
setup = function(){
  createCanvas(400, 400);
  noStroke();
  colorMode(HSB, 255);
};
draw = function(){
  var pos, dim, k, i$, i, f, offsetPosX, offsetPosY, offsetWave, offsetDim;
  clear();
  background(245);
  pos = basePos;
  dim = baseDim;
  k = 0;
  for (i$ = 255; i$ >= 0; --i$) {
    i = i$;
    fill(i, 255, 225);
    stroke(i, 220, 255);
    f = (frameCount * 0.05) * -1;
    offsetPosX = sin(pos * 0.02) * 100;
    offsetPosY = cos(pos * 0.02) * 100;
    offsetWave = sin(i * 0.5 + f) * 10;
    offsetDim = sin(pos * 0.02) * 40;
    ellipse(200 + offsetPosX + offsetWave, 200 + offsetPosY, 50 + offsetDim, 50 + offsetDim);
    pos++;
  }
  basePos++;
  baseDim++;
};
mousePressed = function(){
  noLoop();
};