// Generated by LiveScript 1.2.0
var dim, setup, draw, mousePressed;
dim = 390;
setup = function(){
  createCanvas(dim, dim);
  noStroke();
  colorMode(HSB, 255);
};
draw = function(){
  var nX, nY, getColor, i$, to$, x, j$, to1$, y;
  nX = 0;
  nY = 0;
  getColor = function(){
    return constrain(Math.round(noise(nX++ + frameCount * 0.01, nY++ + frameCount * 0.02) * 350), 0, 255);
  };
  for (i$ = 0, to$ = dim; i$ <= to$; i$ += 6) {
    x = i$;
    for (j$ = 0, to1$ = dim; j$ <= to1$; j$ += 6) {
      y = j$;
      fill(getColor(), 210, 240);
      rect(x, y, 6, 6);
    }
  }
};
mousePressed = function(){
  noLoop();
};